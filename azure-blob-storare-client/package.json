{
  "name": "azure-blob-storage-client",
  "version": "0.0.2",
  "description": "package for upload base64 image to Azure blob storage",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build:clean",
    "clean": "rm -rf ./dist",
    "test": "exit 0",
    "coverage": "exit 0",
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "build:debug": "npm run clean && npm run build -- --sourceMap",
    "build:clean": "npm run clean && npm run build",
    "build:watch": "tsc -w"
  },
  "contributors": [
    "Italo José <italo@live.com>"
  ],
  "types": "dist/index.d.ts",
  "license": "GPL-3.0",
  "dependencies": {
    "@azure/storage-blob": "^10.5.0",
    "tsyringe": "^3.3.0"
  },
  "devDependencies": {
    "@types/bson": "^4.0.0",
    "concurrently": "^4.1.1",
    "ts-node": "^8.3.0",
    "tslint": "^5.18.0",
    "tslint-config-standard": "^8.0.1",
    "tslint-eslint-rules": "^5.4.0",
    "typescript": "^3.6.4"
  },
  "files": ["dist"]
}
